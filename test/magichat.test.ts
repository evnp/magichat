import { magicHatBegin } from "../src/core";

describe("Magic Hat", () => {
  test("TODO", () => {
    expect(
      magicHatBegin("seed", () => {
        /* pass */
      })
    ).not.toBe(null);
  });
});
